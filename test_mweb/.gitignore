# Ignore macOS system files
.DS_Store

# Google API credentials
credentials.json
token.json
storageState.json

# 가상환경 폴더 무시 (예: pyp, venv, env 등)
pyp/
venv/
env/

# Python 바이트코드 캐시 무시
__pycache__/
*.py[cod]
