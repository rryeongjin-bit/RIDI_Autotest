
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 기본 실행 옵션
addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html

# 커스텀 CLI 옵션 
# markers =